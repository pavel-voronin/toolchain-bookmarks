{
  "name": "toolchain-bookmarks",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build --compile --outfile dist/bookmarks src/cli.ts",
    "dev": "bun run src/cli.ts",
    "check": "bunx tsc --noEmit",
    "test": "bun test tests/integration"
  },
  "dependencies": {
    "cac": "^6.7.14",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "bun-types": "^1.3.9",
    "typescript": "^5.8.2"
  }
}
